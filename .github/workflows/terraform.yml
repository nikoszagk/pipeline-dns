name: Terraform DNS

on:
  pull_request:
  push:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read

env:
  ARM_USE_OIDC: true
  ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
  ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
  TF_INPUT: false
  TF_VAR_app_ip: ${{ vars.TF_VAR_APP_IP }}
  TF_VAR_app_cname: ${{ vars.TF_VAR_APP_CNAME }}
  TF_VAR_app_txt: ${{ vars.TF_VAR_APP_TXT }}
  TF_VAR_subscription_id: ${{ vars.TF_VAR_SUBSCRIPTION_ID }}
  TF_VAR_dns_rg_name: ${{ vars.TF_VAR_DNS_RG_NAME }}
  TF_VAR_location: ${{ vars.TF_VAR_LOCATION }}
  TF_VAR_zone_name: ${{ vars.TF_VAR_ZONE_NAME }}
  TF_VAR_storage_account_name: ${{ vars.TF_VAR_STORAGE_ACCOUNT_NAME }}


jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform init
        run: terraform init -upgrade

      - name: Terraform fmt & validate
        run: |
          terraform fmt -check
          terraform validate

      - name: Terraform plan
        run: terraform plan -no-color

      - name: Terraform apply (main only)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -no-color
